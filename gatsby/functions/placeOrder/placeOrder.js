//require nodemailer
const nodemailer = require('nodemailer');
//create transport for nodemailer
const transporter = nodemailer.createTransport({
    //all authentication variables you need in order to connect to it
});